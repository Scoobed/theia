# Wrapper: adds /sbom.spdx.json and /LICENSE to zookeeper.
ARG UPSTREAM_TAG=latest

FROM docker.io/zookeeper:${UPSTREAM_TAG} AS upstream

FROM alpine:latest AS sbom-generator
RUN apk add --no-cache curl
COPY --from=upstream / /upstream/
RUN curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | \
    sh -s -- -b /usr/local/bin && \
    cd /upstream && syft . \
      --exclude './proc' \
      --exclude './sys' \
      --exclude './dev' \
      -o spdx-json=/sbom.spdx.json

FROM docker.io/zookeeper:${UPSTREAM_TAG}

LABEL maintainer="Antrea <projectantrea-dev@googlegroups.com>"
LABEL description="theia-zookeeper: zookeeper with SBOM and LICENSE."

COPY --from=sbom-generator /sbom.spdx.json /sbom.spdx.json
COPY LICENSE /LICENSE
